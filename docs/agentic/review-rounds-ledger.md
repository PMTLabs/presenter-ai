# Review rounds ledger

One row per external review round (agent-research skill). A = class fix missed sites · B = oracle too narrow · C = claim unsupported by code · D = genuine defect/gap.

| Date | Branch / ticket | Round | A | B | C | D | Note |
|---|---|---|---|---|---|---|---|
| 2026-09-21 | plan 002 (phase 0a port) — plan review, pre-implementation | 1 | 1 | 1 | 4 | 3 | protocol field name, HTTP shapes and parallel-path claims were unsupported by code; pump oracle too narrow; concurrency model unspecified — all folded in before approval |
| 2026-09-21 | feature/002-dotnet-core-port — impl review T2–T6/T9 (`326cab7..2f22c58`) | 1 | 0 | 7 | 0 | 2 | secrets-guard class too narrow (whitespace/case bypass) and traceparent not guaranteed on one producer; seven ported tests weaker than their Node/spec oracles — all folded in, see docs/review/002 |
| 2026-09-21 | feature/002-dotnet-core-port — impl review T7/T8/T10/T12 + review-1 fixes (`2f22c58..906af83`) | 2 | 0 | 2 | 0 | 3 | round-1 classes held at every site; new D: no live session is ever disposed (class: `ILiveSession` not disposable), fire-and-forget `EndAsync` unobserved, guard boundary vs `\u`-escaped JSON names (documented as out of class); B: stop-after-slide oracle (Node parity — claim corrected in place), backpressure test drove the seam not the channel — see docs/review/003 |
| 2026-09-21 | feature/002-dotnet-core-port — impl review T13/T14 web + review-2 fixes (`906af83..41d093a`) | 3 | 0 | 3 | 0 | 4 | new scope (React page): mic/audio not released on route teardown, StrictMode double deck load, error results ignored / `errorMessages` unused; three web oracles too narrow (backoff schedule, text-message parsing, playback start) — round-2 classes held at every site; see docs/review/004 |
| 2026-09-22 | plan 004 (identity, persistence, auth-state Redis) — plan review, pre-implementation | 1 | 0 | 0 | 1 | 10 | C: "exactly two `StartAsync` call sites" ignored nine test files and the file repository that implements the interface being changed; D: pre-auth sockets could seize the presenter slot, refresh rotation not atomic, persisted Markdown lost the context file, recorder had no upstream metadata or completion barrier, unverified-email linking, `is_disabled` never enforced, CLI persistence unwired, PR split broke the client and the OpenAPI drift test — all folded in before approval, see docs/review/005 |
| 2026-09-22 | feature/004-identity-persistence — impl T8/T11b, orchestrator review of the implementing agent's first pass (`4159e7e`..working tree) | 1 | 2 | 5 | 0 | 1 | A: "update every harness" was done for the CLI and API tests but replaced by four ownerless shims in `Presenter`/`FilePresentationRepository` for the rest, and the F4 split reached the interface but not the API's DI registration; B: API test double ignored the owner (bridge passing a wrong owner kept 73/73 green), 404 test could not tell "not yours" from "missing", `PresenterStartResult.Upstream` unasserted, no Library success path, golden test discarded the envelope; D: `/v1/presentations` had no paging (silent 25-row cap) — all fixed in round 2 and mutation-verified by the orchestrator |
| 2026-09-22 | feature/004-identity-persistence — impl T9/T10, orchestrator review of the implementing agent's first pass (`385b2f5`..working tree) | 1 | 0 | 4 | 1 | 2 | D: the recorder's idempotency held at the public `EndAsync` but not in the worker, so a queued `Closed` plus disconnect cleanup finalised the row twice (the second write overwrote the upstream's reason and billed seconds after the slot was released); an end arriving between a queued and a processed begin dropped the row of a run that really started. B: integration tests waited on a row count that is true at begin, not at finalisation; the real `SessionRecorder.Detach` was untested because the API double replaced it; the fallback test could not tell which session id was stored (both routes answered `sess_fake`); a negative assertion after a fixed 100 ms sleep. C: an interface summary had moved onto a record. Sent to a fresh agent as round 2 along with `run --owner` |
| 2026-09-22 | feature/004-identity-persistence — PR #3 external impl review, identity (`origin/develop...8199b44`) | 1 | 2 | 3 | 1 | 4 | C: T12's `/v1` no-store/X-Request-Id half was marked done but never landed; A: the 401 interceptor bypasses the single-flight refresh (sign-out race on reload), binding failures bypass the error catalogue; D: pre-auth frames unbounded, server-initiated closes wait forever on a silent peer (also holds the slot via the 1011 path), CORS only on refresh; B: constant rate-limit headers, no negative JWT cases, validator fixture too thin — see docs/review/006 |
| 2026-09-22 | feature/004-identity-persistence — PR #3 external impl review, persistence (`origin/develop...8199b44`) | 1 | 1 | 3 | 1 | 5 | D: a start queued but not yet dequeued is invisible to disconnect cleanup (and `RequestAborted` cancels the observer, not the start), so a real run goes unrecorded; CLI crashes on an unreachable DB; `run` file graph lacks scope validation; index model vs migration drift. C: first-run runbook has no JWT settings anywhere. A: import confinement lexical only. B: migration oracle = table names, vacuous failed-start wait, unknown-owner test queries an opaque id with an email. Recorder best-effort on DB outage disputed as design (D8) — see docs/review/006 |
| 2026-09-22 | feature/004-identity-persistence — PR #3 external impl review round 2, identity (fix diff `032f2eb..06537c3`) | 2 | 0 | 7 | 0 | 3 | D (each lowered to improvement after re-trace, all fixed): sign-out during an in-flight refresh does not stick (the rotated successor survives logout; predates the fix); the 401 interceptor would replay the one-time SSO code; rate-limit headers chosen by path suffix, not endpoint policy. B: oracles sample one route/case (CORS, 413/415, rate-limit policies, deceptive loopback hosts, base URL edges); skew pinning disputed; 500/streaming header oracle deferred — see docs/review/007 |
| 2026-09-22 | feature/004-identity-persistence — PR #3 external impl review round 2, persistence (fix diff `032f2eb..06537c3`) | 2 | 1 | 3 | 0 | 2 | D disputed: 1009 close racing the writer (`ManagedWebSocket` serialises sends via `_sendMutex`); 90 s start bound past-expiry behaviour is D8 best-effort, not a regression, but its comment's justification ("four routes", "ten-second load") was false (fixed). A: import's per-file catch swallowed DB outages (now stops at the first). B deferred: sampled catalog oracle, failed-start barrier, connectivity boundary test — see docs/review/007 |
| 2026-09-22 | feature/005-echo-and-stall — PR #5 external review, plan 005 + A1 (`origin/develop...6e3a7ba`) | 1 | 1 | 1 | 2 | 4 | D: echo-gate coupling learned from pre-echo/far-pause frames (fixed: learn only after a full far tail), recovered delegation rejection raised as an error, loopback fallback overwritten while the context resumes, second question behind a pending backend (rejected as design: the hold spans all open questions, 15 s cap). A: late delegation could append answer-now while paused (fixed; navigation case kept by design). C: top-level `backend_error` unhandled (now handled), README one-nudge row. B: timestamp test never reached the guard — see docs/review/008 |
| 2026-09-23 | plan 007 (voice control + tool protocol) — plan review, pre-implementation | 1 | 1 | 1 | 2 | 10 | D: tool call through IPresenter could deadlock the loop; no live-model delegation policy; single pending marker cannot track tool rounds; no stale/exactly-once contract; one flush does not stop late narration; echo could run commands; undefined check-in/end phases and precedence; mutable tool list; fragile utterance assembly. A: paused listening fixed at one of three audio gates. B: catalogue size oracle too narrow. C: ineffective mutation, docs task without verify — all folded into revision 1, see docs/review/009 |
| 2026-09-23 | plan 008 (MCP external tools) — plan review, pre-implementation | 1 | 0 | 4 | 2 | 9 | D: credential-bearing auto-redirects; retry after 401 could repeat a write; DCR-only OAuth narrowed the brief (spec 2026-07-28 prefers client ID metadata documents); cross-instance refresh race; orphaned Start loader; approved result announced after navigation; redaction not designed end to end; NAT64 network-specific prefixes; confirmation output outside the tracker. B: connected-address, OAuth, secret-scan and owner hand-off oracles too narrow. C: impossible cross-user take-over test, false SSO precedent — all folded into revision 1, see docs/review/010 |
| 2026-09-23 | feature/007-voice-control-and-tools — implementation review round 1 (`develop...368220a`) | 1 | 0 | 1 | 2 | 12 | D: cooperative-only tool timeout; stale navigation result left the tool barrier stuck; tool rounds did not re-arm the 15 s hold; out-of-range local go-to silent (AC3); late tool call while Ending; ignored submit results; check-in no dropped the hold; single long delta passed as a command; mutable exported definitions; premature confirmed:true ok:true. Rejected: echo transcript before its own output (impossible), command-phrase log privacy (plan-specified). B: budget measured the list, not the start frame. C: stale guide flow, off-loop log — see docs/review/011 |
| 2026-09-23 | feature/007-voice-control-and-tools — implementation review round 2 (`develop...9f25d91`) | 2 | 1 | 2 | 1 | 9 | D: check-in no still resumed after 15 s; navigation exemption outlived a later navigation; range reply dropped the hold; same-slide tool skipped the queued command; serialised 4 KiB cap; registry race; disposed arguments on a timed-out call. A: output gate fixed for Paused, missed Ending. B: range reply asserted the instruction, not audio; round 1 oracles too narrow. C: guide default tool list. Deferred D-01 (only built-ins reach IPresenter), rejected D-04 (refusal only on a closed session) — see docs/review/012 |
| 2026-09-23 | feature/007-voice-control-and-tools — implementation review round 3, confirmation (`088ecf1`) | 3 | 0 | 0 | 1 | 3 | D: continue/resume after check-in "no" never restarted narration (blocker); range-reply flag leaked into the next question; every tool fault logged as late. C: guide kept the wrong final-answer timer. Escalation round: all four fixed in one pass, no round 4 — see docs/review/013 |
| 2026-09-23 | feature/008-mcp-external-tools — implementation review round 1 (`0218363...04ebf24`) | 1 | 1 | 4 | 2 | 7 | D: confirmed action could be replayed after a 401 (substring method check, blocker); byte-budget omission left tools undiscoverable; OAuth reconnect ignored the stored pre-registration; completion did not list tools; refresh failure could overwrite the winner; untrusted labels logged raw; tools API showed the override, not the effective permission. A: auth-failure status mapped differently per route. B: named-client pipeline, 404 retry count, completion listing, badge combination. C: no-key no-auth claim, start budget — see docs/review/014 |
| 2026-09-23 | feature/008-mcp-external-tools — implementation review round 2 (`b87ab30...64580bb`) | 2 | 1 | 3 | 2 | 2 | D: byte-overflow discovery could throw for near-budget pinned tools (narrowed to an invariant test); effective permission cached on the Tools page after a server switch. A: credential-version fencing applied to the failed refresh only, not the other status writers. B: refresh race tested at the repository, guard seam ignored the dialed IP; a third (byte overflow untested) rejected, the test exists. C: per-server budget wording, byte-overflow note — see docs/review/015 |
| 2026-09-23 | feature/008-mcp-external-tools — implementation review round 3, confirmation (`64580bb...1095717`) | 3 | 0 | 0 | 0 | 2 | D: no-credential disconnect not fenced by the nullable version; post-refresh outcomes fenced by the pre-refresh version. Escalation: third round on advisory status fencing; status never gates tool loading, both races display-only and self-healing; user deferred both as known limitations, no round 4 — see docs/review/016 |
| 2026-09-23 | feature/009-billing-guards — plan review round 1 (draft plan 009) | 1 | 1 | 3 | 1 | 4 | D: cap armed at connect contradicted A1; reconnect held the single reader so End/cap could not interrupt it (blocker); MCP cancellation during refresh/reconnect still hit the bare catch; end-reason vocabulary missed bridge abort reasons. A: close paths lacked one reason mapping. B: guard, heartbeat and reconnect tests asserted final state only. C: ApiFactory fake-clock seam assumed, not existing — see docs/review/017 |
