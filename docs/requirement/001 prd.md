I want to build a small utility tool that can act as the presenter to present the provided slide deck. It can be PowerPoint, can be a slide deck in the HTML format, and I can provide the context in a MD file. Then the tool will call the voice model to present. So the voice model will be the duplex mode, which means it can listen and can present. I am targeting to the GPT Live model, so do a research and propose a solution and implement. I want a quick workable version first since I am in the rush to rehearse my presentation, so I need that as soon as possible.

Technology stack:

Node for both frontend and backend:
- slide in html, the app needs to drive the html slide
- script will be provide in a predfined format so the app will know how to drive 
- backend will communicate with upstream voice (duplex mode) model.

endpoint and apikey of upstream model will be put in .env