# 003 — InkSpoke web kit inventory (scout for plan 002 T13)

**Scout:** `pi --model openai-codex/gpt-5.6-luna:medium`, read-only, 2026-09-21. Source: `D:/sources/work/inkspoke/inkspoke-main/web` at commit `b83e691f`. Feeds the T13 brief; the open questions at the end were decided by the orchestrator in that brief.

- Request: `impl002-SCOUT-WEB`
- Scope: read-only inventory of `D:\sources\work\inkspoke\inkspoke-main\web`, excluding `node_modules` (and not opening any `.env`).
- Origin commit for copied-origin headers: `b83e691f` (`git rev-parse --short HEAD`). The supplied partial report said `899f9020`; that hash was stale relative to the checked-out HEAD and is corrected here.

## Workspace layout

- Root `web/package.json:1-22` is private workspace `inkspoke-web`, with workspaces `shared`, `site`, `admin`. Scripts are `dev:site`, `dev:admin`, concurrent `dev`, `build:site`, `build:admin`, sequential `build`, `lint:site`, `lint:admin`, and sequential `lint` (`web/package.json:5-16`). Root dev dependencies are `concurrently ^9.1.2` and `typescript ^5.7.3` (`web/package.json:17-20`).
- `web/bun.lock:1-18` is Bun lockfile v1/config v1 and records the workspace manifests. No Bun runtime version or `engines` declaration was found in the inspected manifests; the lockfile does not establish a runtime version.
- `web/tsconfig.base.json:2-19` is strict TypeScript: ES2022/ESNext, bundler resolution, React JSX, declarations/maps/source maps, `isolatedModules`, `skipLibCheck`, JSON modules, consistent casing, and the `@inkspoke/shared/* -> ./shared/src/*` path. It excludes `node_modules`.
- `@inkspoke/shared` (`web/shared/package.json:1-25`) is ESM 0.1.0, exposes source entrypoints `.`, `./types`, `./api`, `./lib` (`:6-11`), has React 18/19 peer support (`:12-14`), and depends on `axios ^1.7.0`, `clsx ^2.1.0`, `tailwind-merge ^2.6.0` (`:15-19`). It has no scripts.
- `@inkspoke/admin` (`web/admin/package.json:1-53`) scripts are Vite dev/build/preview, `eslint . --max-warnings 22`, `vitest run`, and watch mode (`:6-13`). Runtime dependencies include workspace shared, React/DOM `^18.3.1`, React Router `^6.28.0`, Zustand `^5.0.0`, Lucide `^0.460.0`, Axios, React Query, forms/resolvers, Zod, Recharts, fflate, clsx and tailwind-merge (`:15-31`). Dev dependencies include Vite `^6.0.0`, Vitest `^2.1.8`, Tailwind `^3.4.0`, ESLint `^10.8.1`, typescript-eslint `^8.66.0`, React plugin, PostCSS/autoprefixer, jsdom and Testing Library (`:33-52`).
- `@inkspoke/site` (`web/site/package.json:1-48`) has Vite-React-SSG dev/build, preview, lint (`--max-warnings 46`) and Vitest scripts (`:6-13`). It uses React 18.3, Router 6.28, Zustand 5, Axios, React Query, forms/resolvers, Zod, Framer Motion, Lucide, clsx and tailwind-merge (`:15-31`), plus Vite 6, `vite-react-ssg ^0.9.1-beta.1`, Tailwind, ESLint/typescript-eslint, Vitest and Testing Library (`:33-47`).
- Admin Vite (`web/admin/vite.config.ts:1-28`) uses React plugin (`:1-2`), `@ -> ./src` (`:16-20`), port **41953**, and `/v1 -> http://localhost:41951` proxy (`:21-27`). It defines `VITE_APP_VERSION` from `process.env.VITE_APP_VERSION` or package version (`:7-13`). Site Vite (`web/site/vite.config.ts:1-78`) uses React, sitemap-drift and robots-gate plugins (`:1-3`, `:68`), the same `@ -> ./src` alias (`:69-73`), SSG route filtering (`:70-84` in the complete file), port **41952**, and `/v1 -> http://localhost:41951` proxy (`:85-93`).
- Admin Tailwind (`web/admin/tailwind.config.js:1-27`) scans `./index.html` and `./src/**/*.{js,ts,jsx,tsx}` (`:3`), uses class dark mode (`:4`), brand colors 50–900 (`:6-17`) and `width.sidebar: 16rem` (`:18-20`), with no plugins (`:23`). Site has the corresponding Tailwind/PostCSS setup (`web/site/tailwind.config.js:1-27`, `web/site/postcss.config.js:1-6`). Admin CSS entry (`web/admin/src/index.css:1-11`) contains the three `@tailwind` directives and tooltip keyframes/class; PostCSS (`web/admin/postcss.config.js:1-6`) loads Tailwind and autoprefixer.
- ESLint is flat config (`web/admin/eslint.config.js:1-93`): JS recommended plus typescript-eslint recommended, React Hooks recommended-latest, React Refresh Vite rules, browser globals, Node globals for config/scripts, and Vitest globals for tests (`:38-93`). Existing backlog rules are warnings (`@typescript-eslint/no-unused-vars`, `no-explicit-any`, `no-useless-assignment`, hooks exhaustive-deps/set-state-in-effect, refresh-only-export), bounded by the package lint script. Site keeps the same structure/rules with its own warning cap (`web/site/eslint.config.js:1-93`).
- Vitest (`web/admin/vitest.config.ts:1-18`) uses React plugin, `@ -> ./src`, globals, jsdom, `src/test/setup.ts`, and `css:false` (`:5-17`); site has the same shape (`web/site/vitest.config.ts:1-18`). Admin package TS config extends the base, emits declarations to `dist`, maps `@/*` and shared and includes Vitest/Testing Library types (`web/admin/tsconfig.json:1-17`); Vite config TS is strict no-emit (`web/admin/tsconfig.node.json:1-11`).

## Kit files to copy

All copied-origin headers should record **InkSpoke commit `b83e691f`**. Exact source line counts: `web/admin/src/components/layout/AdminLayout.tsx` **216**, `AdminRoute.tsx` **9**, `src/components/ui/ThemeToggle.tsx` **40**, `src/index.css` **11**, `tailwind.config.js` **27**, `src/stores/authStore.ts` **41**, `web/shared/src/api/client.ts` **77**, and `web/shared/src/lib/cn.ts` **6**.

- `web/admin/src/components/layout/AdminLayout.tsx:1-216`: imports React state, Router `NavLink`/`Outlet`/unused `useLocation`, 17 Lucide icons, shared `cn`, local auth store, and `ThemeToggle` (`:1-29`). It supplies a responsive/collapsible sidebar, InkSpoke-specific nav groups/labels/routes, logout, header and outlet. Closed runtime imports are this file, `ThemeToggle`, `authStore`, `@inkspoke/shared`’s `cn`, `react-router-dom`, `lucide-react`, and React. It imports no button/card primitive; cards are inline Tailwind. The InkSpoke branding and nav list (`:31-81`, `:112-204`) must be replaced.
- `web/admin/src/components/layout/AdminRoute.tsx:1-9`: imports `Navigate` and local auth store (`:1-2`); redirects unauthenticated/non-admin users to `/login` (`:5-7`), otherwise renders children (`:8`). Closure is Router plus auth store and React JSX runtime.
- `web/admin/src/components/ui/ThemeToggle.tsx:1-40`: React hooks and Sun/Moon from Lucide (`:1-2`); reads `localStorage` and `matchMedia` (`:5-10`), toggles document-root `dark` class and persists `theme` (`:13-22`), with no local dependency.
- Theme/CSS files: `web/admin/tailwind.config.js:1-27`, `web/admin/postcss.config.js:1-6`, and `web/admin/src/index.css:1-11`. The theme is class dark mode, brand blue palette 50–900 and 16rem sidebar, with tooltip animation and no plugins. There are no reusable button/card primitives required by the three kit components. Optional inline-card example is `web/admin/src/components/shared/StatCard.tsx:1-47` (imports only `cn` and two Lucide icons).
- Auth store: `web/admin/src/stores/authStore.ts:1-41` is a persisted Zustand store. `AuthUser` is `{id,email,displayName?,tier,isAdmin}` (`:4-10`); state stores access/refresh tokens, user and auth flag and exposes setters, `login`, and `logout` (`:12-28`); persistence name is `inkspoke-admin-auth` (`:31-33`). There is no current-user/bootstrap fetch. Login obtains tokens/user through `web/admin/src/pages/auth/AdminLogin.tsx:1-43`, rejects non-admins, then calls `login`. Site has a near-identical store with `inkspoke-auth` (`web/site/src/stores/authStore.ts:1-41`).
- Shared API pattern is **Axios, not openapi-fetch**: `web/shared/src/api/client.ts:1-77` creates an Axios instance (`:11-13`), adds Bearer access tokens (`:29-36`), queues concurrent 401 requests (`:38-50`), posts refresh to `${baseURL}/v1/platform/auth/refresh` (`:59-63`), updates tokens/retries (`:65-68`), and logs out on refresh failure (`:69-73`). Admin wires it using `import.meta.env.VITE_API_URL || ''` and store callbacks (`web/admin/src/api/client.ts:1-15`). Shared exports the client and `cn` from `web/shared/src/index.ts:1-12`.
- No `openapi-typescript`, `openapi-fetch`, `generate-client.ts`, generated OpenAPI file, `ProblemDetails`, `errorCodes.ts`, or `errorMessages.ts` exists in the non-`node_modules` source inventory. `web/admin/src/lib/errors.ts:1-20` is only an ad hoc Axios error-message extractor, preferring `error`, `title`, `message`, transport message, then fallback (`:8-19`). Plan T13 explicitly requires presenter-ai to add generated client/types, error-code union, `isProblem`, and error message map (`D:\sources\demo\presenter-ai\docs\plan\002-phase0-dotnet-core-port-api-web.md:482-491`; conventions §12 `D:\sources\demo\presenter-ai\docs\reference\001-api-and-code-conventions.md:213-218`).

## Conventions observed

- Folder naming is mixed by app: admin uses `src/pages/` with feature subfolders and shared UI under `src/components/{layout,shared,ui}`; site also uses `src/pages/` plus `src/components/{auth,layout,sections,ui}` (`web/admin/src`, `web/site/src`). T13 names new presenter app routes explicitly (`D:\sources\demo\presenter-ai\docs\plan\002-phase0-dotnet-core-port-api-web.md:498-506`), so use `web/app/src/routes/` rather than copying InkSpoke's page-heavy taxonomy.
- State is Zustand, not Context: auth stores call `create` and `persist` (`web/admin/src/stores/authStore.ts:1-2,30-35`; `web/site/src/stores/authStore.ts:1-2,30-35`). React Query is installed and admin's root wraps the app in `QueryClientProvider` (`web/admin/src/main.tsx:4-15`), but route/auth state remains Zustand.
- Routing uses React Router 6 declarative `<Routes>`/`<Route>` rather than data routers (`web/admin/src/App.tsx:2,47-94`). Pages are lazy-loaded (`web/admin/src/App.tsx:7-32`) and protected routes nest beneath `AdminRoute` + `AdminLayout` (`:49-54`). The sample test uses `MemoryRouter` and `QueryClientProvider` (`web/admin/src/__tests__/routing.test.tsx:11-22`).
- Environment/base URL handling is Vite `import.meta.env`: admin passes `VITE_API_URL || ''` (`web/admin/src/api/client.ts:4-15`), while dev requests can use the `/v1` proxy (`web/admin/vite.config.ts:21-27`); site has the same proxy (`web/site/vite.config.ts:85-93`). Do not copy endpoints unchanged: T13 requires app port **47914** and proxies `/api`, `/ws`, `/decks`, `/health` to **47913** (`D:\sources\demo\presenter-ai\docs\plan\002-phase0-dotnet-core-port-api-web.md:482-491`). No `.env` was opened.
- Tests are colocated as `*.test.ts`/`*.test.tsx`, often under `__tests__` (for example `web/admin/src/__tests__/routing.test.tsx:1`, `web/admin/src/components/__tests__/ErrorBoundary.test.tsx`, and `web/site/src/data/blog/blog.test.ts`). The routing test imports Vitest, Testing Library, React Router, and Zustand (`web/admin/src/__tests__/routing.test.tsx:1-10`), uses setup cleanup (`web/admin/src/test/setup.ts:1-5`), and verifies auth-gated routes plus robots (`routing.test.tsx:46-76`).
- Lint names are root `lint`, `lint:site`, `lint:admin` (`web/package.json:14-16`) and package `lint` scripts with warning caps (`web/admin/package.json:10`, `web/site/package.json:10`). Build names are root `build`, `build:site`, `build:admin` (`web/package.json:10-13`) and package `build` (`web/admin/package.json:7`, `web/site/package.json:7`).
- `bun run build` outputs `web/admin/dist` as a browser SPA with `index.html`, public redirect/header/robots files and hashed `assets/*.js`/`*.css` (existing `web/admin/dist/index.html`, `web/admin/dist/assets/index-Cc1tM9Qr.js`). It outputs `web/site/dist` with prerendered route HTML, hashed assets, sitemap, and static-loader-data (existing `web/site/dist/index.html`, `about.html`, `assets`, `static-loader-data`). Presenter `web/app` should follow the admin SPA shape unless later specified otherwise.

## Recommended copy plan for presenter-ai

Target names follow T13: `web/{shared,app,admin}` with React 19 and generated OpenAPI client. Preserve origin headers as **InkSpoke b83e691f**.

- `inkspoke/web/package.json` → `presenter-ai/web/package.json`: retain Bun workspaces, change workspaces to `shared`, `app`, `admin`, update root scripts to target `app`/`admin`, and add the OpenAPI generation/check command required by T13.
- `inkspoke/web/tsconfig.base.json` → `presenter-ai/web/tsconfig.base.json`: retain strict/bundler settings; rename `@inkspoke/shared/*` to `@presenter/shared/*`.
- `inkspoke/web/shared/package.json` → `presenter-ai/web/shared/package.json`: retain ESM shape but replace Axios API dependency with `openapi-fetch`, add `openapi-typescript` where generation is owned, and use React 19 peer range.
- `inkspoke/web/admin/src/components/layout/AdminLayout.tsx` → `presenter-ai/web/admin/src/components/layout/AdminLayout.tsx`: copy responsive/collapsed behavior, replace InkSpoke logo/nav/routes/icons, and update imports to `@presenter/shared`.
- `inkspoke/web/admin/src/components/layout/AdminRoute.tsx` → corresponding presenter path: copy guard shape, aligning the capability check with presenter auth.
- `inkspoke/web/admin/src/components/ui/ThemeToggle.tsx` → corresponding presenter path: copy as-is apart from branding if desired.
- `inkspoke/web/admin/tailwind.config.js`, `postcss.config.js`, `src/index.css` → corresponding presenter admin paths. Retain brand tokens and dark-mode convention; no button/card primitive is required by the copied layout kit.
- `inkspoke/web/shared/src/lib/cn.ts` → `presenter-ai/web/shared/src/lib/cn.ts`; `shared/src/index.ts` → corresponding shared index after adding API/error exports.
- Do **not** copy the Axios clients as the presenter API client. Create `presenter-ai/web/shared/src/api/generated.ts` (checked-in output), `src/api/client.ts` (openapi-fetch factory), and `web/shared/scripts/generate-client.ts` from `/openapi/v1.json`; add `errorCodes.ts`, `isProblem`, and `errorMessages.ts` per conventions §12 (`D:\sources\demo\presenter-ai\docs\reference\001-api-and-code-conventions.md:213-218`).
- Move the auth concept from `web/admin/src/stores/authStore.ts` to intended T13 location `presenter-ai/web/shared/src/auth/authStore.ts` (`D:\sources\demo\presenter-ai\docs\plan\002-phase0-dotnet-core-port-api-web.md:482-491`), removing InkSpoke fields/storage names and using presenter auth/dev-sign-in behavior.
- `inkspoke/web/admin/vite.config.ts` and `vitest.config.ts` → presenter admin paths; create `presenter-ai/web/app/vite.config.ts` from the same kit with port 47914 and proxies `/api`, `/ws`, `/decks`, `/health` to `http://localhost:47913`, plus app Vitest setup.
- Strip InkSpoke-specific pages, labels, routes, logos, API paths, marketing/SSG plugins and the site workspace. Do not copy `web/site` wholesale: presenter target is `app`, not `site`.
- Minimal admin placeholder: retain `main.tsx`, provider/router shell, `AdminRoute`, `AdminLayout`, CSS/theme and one authenticated `/` route rendering `ComingSoon`. A small centered heading plus “Presenter admin coming soon” using Tailwind is sufficient. Existing site `ComingSoon` is visual reference only (`web/site/src/components/ComingSoon.tsx:1-118`) and should not carry InkSpoke email/branding.

## Files examined

- Brief/plan/conventions: `D:\sources\demo\presenter-ai\docs\plan\002-phase0-dotnet-core-port-api-web.md:478-506`; `D:\sources\demo\presenter-ai\docs\reference\001-api-and-code-conventions.md:207-220`.
- Workspace manifests/config: `web/package.json`, `web/bun.lock`, `web/tsconfig.base.json`, `web/shared/package.json`, `web/admin/package.json`, `web/site/package.json`, package TS configs, Vite/Vitest/ESLint/Tailwind/PostCSS configs.
- Kit/runtime sources: admin layout/route/theme/CSS/auth/API/errors/App/main/test setup/routing test; shared API/cn/index; site auth store and ComingSoon.
- Output inventory: existing `web/admin/dist/**` and `web/site/dist/**` layouts were inventoried without running a build. `node_modules/**` was ignored. No `.env` file was opened.

## Open questions

- What exact presenter admin routes/roles replace InkSpoke's `isAdmin` check, and should app use the copied admin nav shell?
- What is the auth/current-user contract, and should tokens persist or use T13 dev sign-in?
- What exact generated-client filename/export shape and `openapi-typescript` version should CI enforce?
- Which presenter Tailwind tokens replace InkSpoke blue, and should app/admin share one config?
- Should production use relative `/api` URLs exclusively, with Vite proxy only in development, or retain a `VITE_API_URL` override?
- Is `web/admin` a real authenticated console now or only the required ComingSoon placeholder in this phase?

