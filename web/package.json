{
  "name": "presenter-ai-web",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": ["shared", "app", "admin"],
  "scripts": {
    "dev": "concurrently -n app,admin -c cyan,magenta \"bun run dev:app\" \"bun run dev:admin\"",
    "dev:app": "cd app && bun run dev",
    "dev:admin": "cd admin && bun run dev",
    "build": "cd app && bun run build && cd ../admin && bun run build",
    "lint": "cd shared && bun run lint && cd ../app && bun run lint && cd ../admin && bun run lint",
    "test": "cd shared && bun run test && cd ../app && bun run test && cd ../admin && bun run test",
    "generate:api": "cd shared && bun run generate-client"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.2",
    "eslint": "^9.17.0",
    "globals": "^15.14.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.19.1",
    "vite": "^6.0.7",
    "vitest": "^3.0.5"
  }
}
