{
  "name": "presenter-ai",
  "version": "0.1.0",
  "description": "Presents an HTML slide deck by voice through GPT-Live-1 (full duplex) from a Markdown script.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server/index.js",
    "dev": "node --watch src/server/index.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "gray-matter": "^4.0.3",
    "ws": "^8.18.0"
  }
}
